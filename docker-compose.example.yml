version: "3.7"
services:
  mailhog:
    image: mailhog/mailhog:latest
    container_name: mailhog
    ports:
      - "8081:8025" # web ui
      - "1212:1025" # smtp server
  # MySQL 5.7
  # Point web servers to mysql for MySQL 5.7 server
  # Username: root
  # Password: root
  # Local Port for Sequel Ace: 3306
  mysql:
    image: mysql:5.7
    container_name: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
    volumes:
      - ./data/mysql:/var/lib/mysql:delegated
    ports:
      - "8082:3306"
    restart: always
    command: mysqld --innodb_strict_mode=OFF
  web:
    build: ./
    restart: always
    depends_on:
      - mysql
    ports:
      - "${PORT:-250}:80"
    links:
      - "mysql"
      - "mailhog"
    environment:
      WORDPRESS_DB_HOST: ${DATABASE_HOST}
      WORDPRESS_DB_USER: ${DATABASE_USER}
      WORDPRESS_DB_PASSWORD: ${DATABASE_PASSWORD}
      WORDPRESS_DB_NAME: ${DATABASE_NAME}
      WORDPRESS_DEBUG: ${DEBUG}
      WORDPRESS_CONFIG_EXTRA: require_once( __DIR__ . '/wp-content/config/project.php' );
    volumes:
      # - ${PWD}/php.conf.ini:/usr/local/etc/php/conf.d/conf.ini
      - ./public/wp-content/config:/var/www/html/wp-content/config:rw
      - ./public/:/var/www/html/wp-config-sample.php:rw
      - ./public/wp-content/uploads:/var/www/html/wp-content/uploads:rw
      - ./public/wp-content/plugins:/var/www/html/wp-content/plugins:rw
      - ./public/wp-content/themes:/var/www/html/wp-content/themes:rw
  cli:
    image: wordpress:cli
    environment:
      WORDPRESS_DB_HOST: ${DATABASE_HOST}
      WORDPRESS_DB_USER: ${DATABASE_USER}
      WORDPRESS_DB_PASSWORD: ${DATABASE_PASSWORD}
      WORDPRESS_DB_NAME: ${DATABASE_NAME}
    depends_on:
      - web
    volumes:
      - db_data:/var/www/html

  # node:
  #   image: node:14
  #   working_dir: /home/node/app
  #   volumes:
  #     - "./public/themes/${PROJECT_SLUG}:/home/node/app:rw"
volumes:
  db_data: {}
